{
  "name": "test-case-to-transaction",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "zgate": "npm run clean && node zgate-script.js && npm run zgate-postman",
    "zgate-postman": "node generate-postman-collection.js output --requestType=zgate --name=Zgate",
    "oneco": "npm run clean && node oneco-script.js && npm run oneco-postman",
    "oneco-postman": "node generate-postman-collection.js output --requestType=oneCo --name=OneCo",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "clean": "rimraf output",
    "clean:test": "rimraf test-output"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "fs-extra": "^11.3.0",
    "glob": "^11.0.3",
    "path": "^0.12.7",
    "uuid": "^11.1.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1"
  }
}
